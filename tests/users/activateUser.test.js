const testUtils = require('../testUtils.js')

test('activate user', async () => {
    const { client, user } = await testUtils.createTestUserAndClient()
    // TODO: test this when we're able to add a user to a company
})
