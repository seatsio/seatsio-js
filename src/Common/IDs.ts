export class IDs {
    own: any
    parent: any
    section: any

    constructor (own: any, parent: any = null, section: any = null) {
        this.own = own
        this.parent = parent
        this.section = section
    }
}
